%h1 Списки
%ul
  - for list in @lists
    %li
      = link_to list.name, list
      = link_to "Edit", edit_list_path(list)
      = link_to "Destroy", list, :confirm => 'Are you sure?', :method => :delete
%p= link_to "Новый список", new_list_path